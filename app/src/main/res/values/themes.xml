<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SimpeCalculator" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/eerieBlack</item>
        <item name="colorPrimaryVariant">@color/eerieBlack</item>
        <item name="colorOnPrimary">@color/eerieBlack</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/eerieBlack</item>
        <item name="colorSecondaryVariant">@color/eerieBlack</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>